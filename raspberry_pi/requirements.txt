# Canon Rebel Photo Booth - Raspberry Pi Requirements

# Image Processing
Pillow==10.0.0
numpy==1.24.3

# HTTP Client
requests==2.31.0

# CUPS Printer Support (optional, installed via system package)
# pycups (usually pre-installed on Raspberry Pi OS)

# System utilities (install via apt)
# gphoto2: sudo apt install gphoto2 libgphoto2-6
# cups: sudo apt install cups
# imagemagick: sudo apt install imagemagick (for RAW conversion)

# Optional: for dashboard integration
python-socketio==5.9.0
