# Photobooth Raspberry Pi Requirements

# Image Processing
Pillow==10.0.0
numpy==1.24.3

# Serial/Bluetooth Communication
pyserial==3.5

# CUPS Printer Support (optional, installed via system package)
# pycups (usually pre-installed on Raspberry Pi OS)

# System utilities (install via apt)
# picamera: sudo apt install python3-picamera
# cups: sudo apt install cups

# Optional: for web dashboard integration
requests==2.31.0
python-socketio==5.9.0
